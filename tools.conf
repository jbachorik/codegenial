# Codegenial Tools Configuration
#
# Format:
#   [tool-id]
#   name = Display Name
#   description = Short description
#   source = Source directory in this repo
#   target = Target subdirectory (relative to install location)

[claude]
name = Claude Code
description = Claude Code agents and commands for build automation and log analysis
source = claude
target = .claude
instructions_file = CLAUDE.md
